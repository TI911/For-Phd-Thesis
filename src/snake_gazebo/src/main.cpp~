/*
 *
 *
 *
 *
 * */

#include "snakebot_gazebo.h"

int main(int argc, char **argv) {
  ros::init(argc, argv, "snakebot_gazebo_node");
  SnakebotGazebo::Initialize();
  ROS_INFO("Initialized : snakebot_gazebo_node");

  ros::Rate loop_rate(50);
  while(ros::ok()) {
    loop_rate.sleep();
    ros::spin();
  }
}
